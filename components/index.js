'use strict';


const Button = require("./button.component");
const Toggle = require("./toggle.component");
const ContextItem = require("./context_item.component");
const Redirect = require("./redirect.component");

module.exports = {
    Button,
    Toggle,
    ContextItem,
    Redirect
}